{
  "name": "lineage",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.15.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:compatibility": "vitest run tests/unit/domain",
    "typecheck": "tsc --noEmit",
    "verify:parity": "node scripts/verify-parity.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
